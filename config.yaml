model:
  provider: deepseek
  model: deepseek-v4-pro

gateway:
  telegram:
    enabled: true
    token: ${TELEGRAM_BOT_TOKEN}
    allowed_chats: "8753553351"

memory:
  type: persistent
  sync_interval: 300

git:
  repo: leaderforge/Hermes-Brain
  branch: master
  sync: bidirectional
  commit_interval: 600
